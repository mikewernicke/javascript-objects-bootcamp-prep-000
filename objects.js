var playlist = {
  Eagles : 'Already Gone',
  Prince: '1999'
}

function updatePlayList(objectList, artistName, songTitle) {
//  return Object.assign({}, objectList, { [artistName] : songTitle } )
}
//function removeFromPlaylist (playlist,artistName) {
//
//}
